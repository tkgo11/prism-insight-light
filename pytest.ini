[pytest]
# Fail instead of hanging forever when a test deadlocks on locks, events, or
# network-bound calls (conftest runs async tests via asyncio.run).
timeout = 120
markers =
    asyncio: run async tests without requiring pytest-asyncio
